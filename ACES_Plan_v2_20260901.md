# ACES Plan v2（2026-09-01）：qmask 证据修订 + 原始仓库基线通道

> **文档定位**：本文**修订**（而非取代）`ACES_Final_Executable_Technical_Plan.md`（下称 v1）。
> **整体验收入口**：项目主线、创新点验证状态、阶段完成总表与修订轨迹的汇总见同级
> `project_progress_status.md`（随节点同步更新）；本文保留全部技术细节与逐次记录（§8 小结）。
> v1 的 §3（Q 场定义）、§6 执行框架、§8 成功标准主体继续有效；本文修订其中
> **§3.3 几何导出公式**（被 Stage2/qmask 证据证伪一处）、**§6 Stage2 门**，
> 并新增 **E 通道（原始仓库外部基线）** 与 **§4 基线体系修正**。
> 触发修订的证据见 §1；qmask 改动的审慎评估见 §2。

---

## 1. 证据盘点（截至 2026-09-01，全部来自本地 run 产物）

### 1.1 各 Stage 完成度

| v1 任务 | 状态 | 产物/数字 |
|---|---|---|
| T0 环境 | ✅ | `aces/env_check.py`，threePM env |
| T1 VALID 原仓库复现 | ✅（8/20，threePM 侧） | **22.93 dB**（对 mean9 proxy，`VALID_FAST_3P_neuron_reproduction_session_notes_2026-08-20.md`） |
| T2 SN2N 原仓库 3D 复现 | ❌ **从未跑过** | 无任何原 SN2N run；只有 ACES 内 `sn2n_xy` 重实现 |
| T3 各向异性剖析 | ✅ | `stage01_3p_repeat_pairability_seed3407`（Q 曲线+深度热图）、`stage01_neurofinder_00_00_proxy` |
| T3b proxy vs 金标准 | ✅ | **Q 转移良好**（Spearman 0.93–1.00，p≤0.001，全轴全分区）；**N 转移失败于 y/z**（y ρ=0.07–0.21，z ρ=−0.26–0.00；x 尚可 0.36–0.87） |
| T4 五档对比（3P，40ep，best ckpt，18 fg patch） | ✅ | valid **22.02** / sn2n_xy **21.98** / iso3d **21.49** / empirical_block **19.87** / ellipsoid **20.33** fg PSNR；平均 best→final **−4.54 dB** 过拟合；iso3d 最稳（−1.94） |
| T4 五档（neurofinder，80ep） | ✅ | aligned fg：valid 14.75 / sn2n_xy 15.10 / iso3d 15.02 |
| T4b CIDC25 标定（H3 核心可证伪主张） | ✅ 闭环（切片1+切片2） | 切片1：r_t 随噪声级收缩 8→3→0；切片2：**H3 被证伪**——实测最优 d*=16/8/64（F1/F2/F3），PSNR(d) 在 d8–d64 平台（~1 dB 内、噪声级无关），Spearman(Q_t,PSNR)≈−1，τ_s 门控预测方向性错误（F3 预测"无合法配对"实测大 d 反而 +10 dB） |
| T5 pair rejection τ 扫描 | ⚠️ 被 qmask 部分吸收 | qmask 只做了 τ 敏感性**记录**，未做系统扫描 |
| T6–T9（L_sc / Us / 深度自适应 / 跨模态） | ❌ 未开始 | — |
| qmask pilot（v1 之外的新实验） | ✅ 完成 | 3p：qmask aligned fg 15.72 vs valid 端点 17.22（**−1.49 dB**，9 patch 偏差>20%，REVIEW）；neurofinder：qmask **17.25** vs sn2n_xy 端点 15.10（**+2.15 dB**，1 patch，REVIEW）；定性复查均无塌缩（3p 轻度过平滑） |

### 1.2 三条最要紧的读数

1. **T3b 的 N 失效是硬证据**：proxy 估的噪声独立性 N 在 y/z 轴上与 9-repeat 金标准不相关/负相关。
   v1 假设"残差 proxy 可分区修正后采信"（R4）——实测**只对 Q（N·S 乘积）成立，对 N 单独不成立**。
   任何在 proxy-only 数据上用 N 阈值做决策的机制（qmask 的 `d_min = min{d: N_a(d)≥τ_n}` 正是）都继承此风险。
2. **v1 §3.3 的几何公式被证伪一处**：`d_a* = max{d: Q_a(d)>τ}` 取"最远的勉强合法间距"，
   在 neurofinder 上退化出 8×8×8 近同性块（Q(d)≥0.6 对 d=1..8 全程成立），且阈值边界脆弱。
3. **五档同骨干对比回答了 H2 的归因问题但留下两个坑**：(a) ellipsoid/empirical 的 −6 dB 级失败
   是采样-损失语义断裂（qmask 已诊断）；(b) 所有下采样档（valid/sn2n_xy/iso3d）存在
   train(半分辨率)/eval(全分辨率) 尺度错配，aligned 评估只是补救而非消除。

---

## 2. qmask 改动的审慎评估（裁决：采纳设计，附 3 项待修）

### 2.1 采纳（有证据支撑的设计修正）

| qmask 改动 | 裁决理由 |
|---|---|
| **区间化几何**：`d_max = max{d: S_a(d)≥τ_s}`、`d_min = min{d: N_a(d)≥τ_n}` 取代单一 `d_a*=max{d:Q>τ}` | 直接修复 §1.2-2 的证伪项。把 Q=N·S 拆回两个因子分别定界，语义正确（N 管"噪声解耦没"，S 管"结构还配不配"） |
| **z 不配对（3P 上 N_repeat(z,1)≈1，各向异性几乎全在 S）** | 与 SN2N 作者既定做法一致（v1 §1.1）；"半径"概念在 z 上是错的对象——qmask 的参与掩码替代半径是正确抽象 |
| **参与掩码 = 连续 VALID↔SN2N 谱**（全开≈VALID；z 关+xy 开≈SN2N） | 把 v1 §3.4 "端点为可比较基准配置"从两张静态配置推广为数据驱动的逐轴决策，与 v1 "data→Q→geometry" 逻辑链同构 |
| **共位相位分裂（stride-1）** | 消除下采样档的尺度错配和"大步长偏移监督不同内容"的语义断裂；aligned==full，评估公平性内建 |
| **实验隔离**（新子包，零改 `ACES/aces/*`，对照档共协议） | 符合 v1 R3 纪律 |

### 2.2 待修（不修不许 scale-up）

| # | 问题 | 证据 | 修法 |
|---|---|---|---|
| **Q-1** | **τ 边界敏感**：3p 的 τ_s 落 0.88–0.90 边界（0.85→全开，0.90→z 关）；neurofinder 的 τ_n 落 0.90–0.95 边界。参与掩码决策是脆弱的 | 两 pilot 的 geometry 报告 | ✅ **已完成**（`qmask/tau_sweep.py` → `qmask/runs/tau_sweep_20260901/`）。关键发现：3p 掩码在 τ_s≤0.88 稳定为 111，0.90 翻转为 .yx；neurofinder 默认 000→提升为 z.x，且 **x 轴参与决策悬在 proxy-N 的 0.0026 边距上**（N(x,1)=0.9474 vs τ_n=0.95）——Q-2 的实证铁证 |
| **Q-2** | **proxy-N 不可靠**：neurofinder 的掩码决策（y 关）恰建立在 T3b 证明不可靠的 proxy-N 上（y 轴 Spearman 0.07–0.21）。这次结果是对的（+2.15 dB），但属侥幸而非稳健 | `proxy_repeat_correlations.json`；τ 扫描 0.0026 边距 | ✅ 代码已实现：`derive_qmask(use_n_decision=False)`（N 仅记录不决策，S+Q 主导）。默认 True 保持原行为；切换前须在 neurofinder 上跑验证 pilot 对比 z.x vs 全开两种掩码 |
| **Q-3** | **qmask 自身 VALID 端点亏 1.49 dB（3p）** | 两轮消融（`qmask_3p_q3_mosaic_seed3407`）：<br>① 假设"mask 填充语义差异"**被否定**（VALID 也是纯 gather）；<br>② 逐位置随机行（mosaic）仅 +0.14 dB（15.72→15.86）；<br>③ **行内槽位随机排列 +0.80 dB（→16.66）**——qmask 固定 view→slot 映射使 (out,target) 偏移方向恒定，模型可学"相位条件化平移"而非去噪（sampling-vs-loss 语义问题的具体形态）；<br>④ 剩余 −0.56 dB 未解释 | ✅ 主因已定位（槽位配对）。**待办**：把槽位随机排列加进 qmask 正式采样器（对 4 角分支同样适用，neurofinder z.x 也受固定配对影响）；剩余 0.56 dB 继续排查（候选：batch 级行一致性、RNG 流差异） |

### 2.3 一个下游修正

3p gate 的"9 个塌缩 patch"判据（pred_mean 对 target_mean 偏差 >20%）用的是 mean9 proxy——
单帧去噪结果对 9 帧均值在前景处天然有强度差，判据在 3p 上系统性偏严。
改为：**塌缩判据 = 输出动态范围塌缩（ptp 比 <0.05，`render_qualitative` 已实现）＋ repeat 噪声统计一致性**，
mean9 偏差降级为参考指标。

---

## 3. 基线体系修正（回应"valid/sn2n_xy 是否用原仓库训练"）

### 3.1 事实澄清

| 档 | 实现来源 | 结论 |
|---|---|---|
| `valid` | **原仓库组件原样 import**：`generate_mask_pair`/`generate_subimages`/`Network_CNR`/`HessianConstraintLoss3D`/`DWT_3D`/`ReadDatasets` 全部来自 `Methods/VALID-v1.0`，零修改；但训练循环（epoch/batch/eval 协议）是 ACES 的 | 方法=原 VALID，**协议≠论文协议**（40ep/18patch pilot 规模） |
| `sn2n_xy` | **重实现**：四相位 XY stride-2 切片（`sampling_aniso.py:77-97`）喂进 VALID 骨干+VALID 损失 | **不是原 SN2N**。原 SN2N 是 XY 对角两体素平均配对 + 自约束损失 + Fourier 上采样管线，均未使用 |
| 原 SN2N 仓库 | `/data2/wjb/SN2N-main/` 在位 | **从未被运行**（T2 欠账） |

### 3.2 原则：两张表，永不混排

- **内部归因表**（已有）：同骨干（CRN+VALID loss+同 eval 协议）、只换采样 → 回答 H2"采样本身是否有效"。
  原始仓库**不进**这张表，否则架构/损失/协议差异污染归因（v1 R3）。
- **外部对比表**（新设 E 通道）：原仓库、原论文协议或注明偏离 → 回答"和已发表方法比怎么样"。
  这是论文表格，v1 的 T1/T2 本来就要求，但 T2 被跳过了。

### 3.3 E 通道任务

| Task | 内容 | 验收 |
|---|---|---|
| **E1（=T2 补课）** | 原 SN2N 3D 在 3P repeat01 + neurofinder.00.00 各跑一次，用原仓库 datagen/trainer，只统一 eval（分层 fg/bg PSNR + SBR） | 两个数据集各产出一行外部表数据 |
| **E2** | VALID 原仓库全协议复算（8/20 的 22.93 dB 换到统一 eval 协议下重报），并补 neurofinder | 外部表 valid 行可 internally comparable |
| **E3** | qmask 最终配置（τ 扫描后）在统一 eval 协议下与 E1/E2 同表对比 | 外部表三行齐全 |

纪律：E 通道跑原仓库时**只允许统一评估端**，不允许为对齐而改原仓库训练代码（v1 R 纪律）。

---

## 4. 执行顺序（v2 版，接替 v1 §6 的当前指针）

> 原则：先修 qmask 的三个待修项（Q-1~Q-3），再谈 scale-up；E 通道与 Q 通道并行；
> CIDC25（H3）在 Stage2 结论稳固后立即启动——它是整篇文章的可证伪支点，已拖最久。

| 序 | 任务 | 依赖 | 验收 |
|---|---|---|---|
| 1 | **Q-3 消融**：qmask 全开 vs valid 的 1.49 dB 分解 | 无 | ✅ 主因定位：行内槽位固定配对（+0.80 dB，3p 单次运行）；⚠️ **方差警告**：nf 上同配置两次独立运行差 1.5 dB（101+fixed：17.25 vs 15.73），单次 pilot delta一律存疑。⏳ 4 臂×3 种子重复矩阵运行中（GPU2，`run_nf_rep_matrix.sh`，~2.2h） |
| 2 | **Q-1 τ 扫描**：两数据集 τ_n×τ_s 网格，掩码稳定性图 | 无 | ✅ 完成（`qmask/runs/tau_sweep_20260901/`） |
| 3 | **Q-2 几何决策修订**：proxy-only 数据 d_min 降级，S+Q 主导 | Q-1 | ✅ 代码实现并修复（`use_n_decision=False` 现在强制 d_min=1 并弃用 N 派生 d_min——第一版只处理 d_min=None，导致 N(d=1)<τ_n 给出 d_min=2、区间不覆盖 d=1、轴被关，q2 pilot 实际仍是 101，形同未改）。单元验证：nf 上 false→111 全开，true→101。⏳ 效果待重复矩阵 |
| 4 | **Gate 判据修订**：塌缩判据改 ptp+repeat 统计（§2.3） | 无 | ✅ 代码实现（`_collapse_audit` 双通道：std 比<0.2 真塌缩；mean 漂移>20% 须伴随 std 比<0.5）；⏳ 待两 pilot 重判 |
| 5 | **E1**：原 SN2N 3D 两数据集 | 无（并行） | ⏳ 双 leg 运行中（3P：GPU3 epoch ~32/100；nf：GPU1 datagen→5ep 训练）。**nf 协议偏离**：nf datagen 产出 ~74.5k 训练块（18,639 batch/epoch，~2.1h/epoch），原仓库默认 100ep 需 ~200h 不可行 → epochs=5，总梯度步数 5×18,639≈93k 与 3P（100×891≈89k）匹配，其余参数不动。⚠️ 上游兼容性发现：SN2N `datagen.py` 的 `random_interchange` 用 `if imgb == []` 判空，numpy≥1.25 下 imgb 为 ndarray 时直接 raise——**原仓库在当前环境无法原样运行**。处理：launcher 内 monkey-patch shim（仅改判空为 `len(imgb)==0`，其余逐行一致，源码零改动）；另发现上游 P2P 增强保存段存在 `append(image_data)`（应为 `image_data_pre`）疑似 bug，P2Pmode=1 时增强数据实际未被保存（行为保持原样未修，记录为上游事实）。⚠️ **存储迁移**：/data2 100% 写满导致首轮 3 任务全挂（checkpoint 截断损坏，torch "unexpected pos" 错）；新产物统一写 `/data3/wjb/Denoising_PM/`（目录结构与 /data2 项目一致，635G 空闲），/data2 只作只读输入。⚠️ **另两处上游不兼容（launcher shim 解决）**：① `Predictor3D` 用 `os.walk` 枚举 model_path 与 img_path——传单文件静默产出空预测，必须传目录；② torch≥2.6 默认 weights_only=True 拒绝其整模型 checkpoint。⚠️ **uint8 契约**：`Predictor3D` 输入仅 `astype('uint8')`，uint16 栈（3P 均值 4449）会模 256 回绕——推理前需离线按 0.1~99.9 分位归一化到 8-bit（datagen 侧有 per-window min-max，训练数据无恙，100ep 模型有效）。**✅ 3P leg 完成：统一 eval（18 fg patch，E2 同协议）fg 16.93 dB**（stack-level 线性对齐后 22.58 仅参考；输出 std_ratio 0.49 偏平滑）。**✅ nf leg 完成（9/3）**：5ep 训练收尾正常（loss ~2.4-3.0，无塌缩），8-bit 归一化推理后统一 patch eval **fg 15.53 dB**（all 15.65 / bg 15.67；全局线性对齐 7.58·pred+24.67；data_range 用 per-patch p0.1–p99.9，与 `_region_metrics` 同尺；2D temporal-mean 目标与 ROI 沿 z 广播，nf 的 z 是时间轴）。参考：内部 qmask nf 重复矩阵（3 seeds，同尺）qm_zx_fix 15.17 / qm_zx_perm 15.08 / qm_all_fix 14.62 / proxy 档 13.84——原 SN2N（5ep×18.6k batch，总步数≈3P 100ep）单次运行略高于 qm_zx_fix，但注意 R10 单次运行方差 ±1.5 dB，不构成结论。**✅ E1 定性验收完成（9/4，数值代理审计 `ACES/logs/audit_e1_numeric.py`，报告 `Results/review_report_e1.html`）**：因当前会话多模态通道失效（Read 返回 base64 非视觉内容，切图也无法目检），改用数值代理替代目检。**⚠️ 用户肉眼复核（9/4）推翻 3P 结论**：nf 基本通过；**3P 判 FAIL——去噪结果明显不对**。根因（用户指出 + 代码核实）：原 SN2N 仓库有 2D/3D 两套管线（`datagen2D/net2D/Predictor2D` 与 `datagen3D/net3D/Predictor3D`），E1 两条 leg 都误用了 3D 管线。nf（2D+time）应走 2D 管线；3P（3D 体数据）虽匹配 3D 管线，但 `P2Pmode=1`（沿 t=z 轴互换）与本项目归因结论冲突（3P 上 N_repeat(z,1)≈1，z 向过采样、配对无独立噪声实现→训练信号弱→过度平滑、细结构丢失，与数值审计 fg Pearson 0.22 一致）；3D 管线内 `P2Pmode=2`（单帧内 xy 配对）才是该数据几何。另核实：Predictor2D 推理逐帧 normalize（与 datagen per-file min-max 训练分布一致）；Predictor3D 输入裸 astype('uint8')（对 8-bit 原生数据等价训练分布，我们的 p0.1–p99.9 归一化近似，非主因）。**处置**：nf 用 2D 管线重跑（`run_e1_nf_2d.sh`，2D 例程规范参数 P2Pmode=1/P2Pup=1/BAmode=1/bs=32/lr=2e-4，epochs 按总步数≈89k 约定）；3P 仅改 P2Pmode 1→2 单变量重跑（`run_e1_3p_p2p2.sh`，其余全同原 3P leg，隔离配对几何变量）。两 leg 原 3D-P2Pmode1 结果保留作对照，不再入外部表 |
| 6 | **E2**：VALID 原仓库统一 eval 复算 | 无（并行） | ✅ 3P 完成：原 VALID（论文协议 100ep）统一评估 **fg 18.53 dB**。⚠️ 重要发现：内部表 valid 档 22.02 比原仓库高 3.5 dB——内部评估含"前景 patch 选择+best ckpt 选择"双重选择偏置，**证实两表必须分开、论文只引用外部表**。**外部表 3P 两行齐**：原 VALID 18.53 vs 原 SN2N 16.93（同 18 fg patch、同 mean9 proxy；SN2N 行含全局线性尺度对齐——自监督输出无绝对尺度，已记录）。**✅ neurofinder 补齐（9/5，小结 #4/#5，经 2D/3D 管线修正重跑）**：nf 行 VALID **23.97**（E2-nf 100ep，E1-nf 同尺 stride-16 协议，无对齐步；附"时间上下文优势"注记——nf 的 z 是时间轴，VALID 3D N2N 看 224 帧上下文而 proxy target 是全栈时间均值，天然奖励上下文平均）vs SN2N-3D-p2p1 15.53 / SN2N-2D 13.71（取哪条待用户裁决，小结 #3）。**3P 行更新**：SN2N 侧采用 P2Pmode=2 重跑 **18.40**（原 p2p1 16.93 降为对照，小结 #1/#4），与 VALID 18.53 基本持平。四行均待用户目检定性图后定稿 |
| 7 | **scale-up 决策**：neurofinder 按 qmask 最优配置 scale；3p 先跑 τ_s=0.90 消融 | 1–4 | ⏳ 前置：重复矩阵（4 臂×3 种子）出均值±范围后再定 scale-up 配置 |
| 8 | **T4b CIDC25**：Q_a(d) 预测最优采样几何（H3），观察 r_t 收缩 | 2–4 | ✅ 第一切片完成（全 1500 帧，`/data3/.../stage01_cidc25_gt_pairability_seed3407/`，`aces/profile_cidc25_pairability.py`）。**读数（fg，τ_s=0.85，S_measurable=噪声数据平滑结构）**：① GT-N≈1.0 全场（仿真噪声逐像素独立）→ **N 门控在合成 GT 数据上无分辨力**；proxy-N 反随噪声级恶化（d=1：0.94→0.90），再次印证 proxy-N 不可靠（T3b 第三个数据集）；② **r_t 随噪声级单调收缩（H3 核心现象首次直接观测）**：r_t=8（F1，t(8)=0.96）→ 3（F2，t(4)=0.81）→ 0（F3，t(1)=0.84<τ_s，全部轴关闭→触发 fallback 提升）；y/x 轴恒 r=1。③ S（GT 结构）噪声级不变、S_measurable 随噪声单调退化——量化了"可测 S"的噪声污染。**下一切片**：✅ 完成（切片2，见 §4-10b 与小结 #12）——**H3 被证伪**：τ_s 门控预测的 r_t 收缩（8→2→0）与实测最优时间配对距离 d*（16/8/64）反相关（Spearman≈−1）；机制=身份捷径（小 d 时 target≈input，模型靠恒等/平均偷懒），非结构相关性 |
| 9 | v1 Stage4–6（L_sc → U_s → 深度自适应）按原序 | Stage2 门过 | 同 v1 |
| 10 | **qmask 创新优化主线**（2026-09-05 起,用户指示）。三个证据锚点：①3P-p2p2 实验——配对几何修正带来 +1.47 dB 且 SN2N≈VALID,证明"逐轴配对几何选择"正是 qmask 核心价值的直接证据;②R9 端点赤字 −1.49 dB 仍在 R10 单次方差(±1.5 dB)量级内,未排除噪声——先以重复矩阵裁决真伪;③外部协议教训——patch PSNR 会被全局对齐+分位 data_range 掩盖感知差异,验收必须加"感知结构"维度（fg Pearson/局部方差相关/SBR 已在 numeric_audit 中,应升级为常规报告列）。子任务：**(a)** 4 臂×3 种子重复矩阵(先 3ep pilot 规模,GPU1/2/3 并行)——同时裁决 Q-2 效果与 R9 真伪;**(b)** ✅ T4b 切片 2 完成(H3 闭环,`qmask/t4b_rgrid.py`,9 个 d 变体×20ep,seed3407+d8 seed3408 复证):**H3 被证伪**——PSNR(d) 陡升 d1→d8 后平台(d8–d64 内 ~1 dB,三档噪声级无关);d*=16/8/64;Spearman(Q_t,PSNR)≈−1;τ_s 门控预测 d_max=8/2/0 与实测反相关。机制:仿真逐像素 i.i.d. 噪声下小 d 的 target≈input 触发恒等/平均捷径,大 d 惩罚捷径强制真去噪——方向与结构相关性理论相反。对 qmask 的启示:几何决策判据需从"结构相关性"重校准为"捷径回避"。定性验收:三行 review 切块已读,d1 明显欠去噪、d8/d64 结构接近 GT、无塌缩伪影。⚠️ R10:论文级主张前需 ≥3 seed 全网格重复(现 1 seed 全网格+d8 单点复证);且仿真 i.i.d. 噪声/空间轴未测,外推受限;**(c)** 依据 (a)(b) 结果设计 qmask 下一步改进:候选=τ_s=0.90 消融(3p)、感知感知的 gate 判据、时间上下文决策轴(nf 上 VALID 23.97 的启发);**(d)** SN2N-3P 补行(低优先,见小结 #6) | 6 | ⏳ (a) 启动中 |

---

## 5. 成功标准与风险（增量）

v1 §8 全部保留，追加：

- **成功标准追加**：参与掩码在 τ 扰动（±0.05）下决策不变，或翻转分支结果均报告；
  外部表与内部表分开呈现，论文主张只引用外部表。
- **R7 基线代际混淆**：把同骨干重实现档的数字与原仓库数字混排一表 → 论文被拒级错误。
  评审时先问"这一行的骨干/协议是什么"。
- **R8 proxy-N 不可靠**（T3b 实证）：任何 proxy-N 单独驱动的决策默认可疑；
  N 阈值只在金标准分支生效。
- **R9 端点赤字未诊断**：qmask 的 VALID-like 端点比真 valid 低 1.49 dB 未解释前，
  一切 qmask 增益解读都要打折扣（Q-3 优先级最高的原因）。
- **R10 单次运行方差**（20260902 实测）：3-epoch pilot 同配置独立进程重跑差达
  1.5 dB（nf 101+fixed：同进程第 4 mode 17.25 vs 独立进程 15.73；疑似 RNG/数据顺序）。
  **规定：任何配置间 delta 必须以 ≥3 种子重复的均值±范围报告；单次 pilot 只用于
  定性判断（塌缩/伪影），不用于定量结论。**

---

## 6. 与 v1 的条款对照

| v1 条款 | v2 处置 |
|---|---|
| §3.3 `d_a*=max{d:Q_a(d)>τ}` | **废止**，改区间化几何（§2.1-1） |
| §3.4 椭球参数化 | 降级为历史消融档保留（ellipsoid 五档之一），不再是推荐配置 |
| §6 Stage3 T5 | 并入 Q-1 τ 扫描（提前执行） |
| §6 T2 | 复活为 E1（此前被跳过） |
| §2.3 塌缩/R2 判据 | 按 §2.3 修订判据定义 |
| 其余全部 | 继续有效 |

---

## 7. 优化推进 Plan（2026-09-06 制定：判据重构 / 抗平滑 / 指标扩展）

> 输入：T4b 切片 2 的 H3 证伪（小结 #12）+ qmask80ep 三 seed 完成（fg 22.14±0.33，最优臂）
> + 用户定性观察（去噪结果偏软、平滑过多，SBR 待提升）。
> 推进原则：每个任务先零训练成本的分析/代码项，再上 GPU 训练项；每项完成即在本节
> 勾选并写读数，重大结论同步进 §8 修改小结；验收一律"先定性后定量"。

### P-A 几何决策判据重构（shortcut-risk 替代 Q/τ_s 门控）

| # | 任务 | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|
| A1 | **shortcut-risk 离线分析**（零训练）：对 F1/F2/F3 计算 r(d)=corr(F_t, F_{t+d}) 与 nMSE(d)=MSE(F_t,F_{t+d})/Var（d∈{1..64}），与实测 PSNR(d)（`distance_summary.json`）对 Spearman，并与 Q_t 对比解释力 | 无 | Spearman(r(d), PSNR(d)) 显著且方向可解释；能解释"陡升 d1→d8 后平台"形状；产出 `metrics/shortcut_risk_analysis.json` | ✅ **但结论为否定性**（9/9）：① r(d)/nMSE(d) 在档内对 d 几乎平坦（F1 r 0.505→0.437，F3 0.039→0.034）——观测量本身对 d 不敏感；② 档内 Spearman(r,PSNR)=−0.82/−0.87/−0.85，与 Q 同为反方向；③ "捷径拉力" pull=nMSE_id−noise_frac 档内也平坦（F1 0.575→0.586）——静态损失差解释不了陡升段；④ 跨档不变量：平台起点恒为 d=8（三档一致），恰与 F0 结构漂移 nMSE0 跨越 ~0.02（d8）重合；跨档上 pull 量级（0.58/0.90/0.97）与 PSNR 增益（+3.4/+7.6/+9.6 dB）正相关。**修正假说：大 d 的收益是优化动力学/梯度信息量问题（target 噪声实现独立性与结构漂移惩罚复制），不是静态损失差**；A2/A3 判据设计须按此修订 |
| A2 | **空间轴闭环**：y/x 平移网格 s∈{0,1,2,4,8,16} 训练变体（复用 `t4b_rgrid.py` 加 spatial-shift 模式），检验结构相关性判据在空间轴是否成立 | A1（判据形式） | H3 空间轴判定（成立/不成立），决定 qmask 是否需要"空间用 Q、时间用 shortcut-risk"混合判据 | 🔄→✅ **pilot 出数**（9/15，20ep×1seed，`qmask/t4b_sgrid.py`，run `stage01_cidc25_sgrid_seed3407`，闭环 `metrics/sgrid_closure.json`）。**读数（eval_best F2 fg）**：s0=17.77（恒等对照≈输入 PSNR）→ s1=**20.45**（峰）→ s2=17.37 → s4=16.86 → s8=18.17 → s16=18.22，非单调、无时间轴式平台。**分档解读**：位移训练对恒等捷径的破除收益随噪声级单调（F3 +5.6 dB（s0 12.5→s16 18.1，类时间轴剂量-响应）、F2 +2.7、F1 ≈0（s0 即 argmax 24.5））；档内最优 s 随噪声级增大（0/1/16）；nMSE0≥0.02 阈值位移 s=1 仅与 F2 平台起点重合；r_noisy(s) Spearman 跨档反号（+0.94/−0.09/−0.83）。**初判**："漂移尺度阈值"跨档不变量不向空间轴迁移；空间位移的价值由捷径危害度（噪声级）决定而非统一阈值——支持 A1 修正假说（优化动力学/恒等吸引子）但否定统一判据形式。**两个混淆已排查**：①平移混淆（模型输出平移 s）用平移补偿 GT 评估证伪（PSNR/SSIM 不变）；②评估/训练分辨率口径与 rgrid 完全一致（rgrid 同口径 SSIM 0.76 正常）。**R10**：空间网格 3 seed 待补；绝对值远低于 80ep 生产 run（20ep pilot 口径），只做网格内剂量-响应比较 |
| A3 | **判据实现**：shortcut-risk 判据写入 qmask 几何决策（`use_n_decision` 路径旁新增），nf/3p 统计上单元验证 + 3ep pilot sanity | A1、A2 | 单元验证决策输出正确；pilot 无塌缩 | ⏳ |
| A4 | **R10 补齐**：rgrid 全网格 ≥3 seed（GPU 并行） | A1 | 三 seed 均值±范围下的 H3 结论不变 | ⏳ |

**A2 空间轴闭环设计（2026-09-14 定稿，零 GPU 部分；训练待 B2/B3 释放卡）**

- **动机**（A1 修正假说的可证伪延伸）：时间轴上结构漂移不可直接观测（高噪淹没），A1 只留跨档不变量"平台起点 d=8 ↔ F0 结构漂移 nMSE0 跨 ~0.02"。空间轴把"漂移"换成**纯几何位移**——结构差可从 F0 精确计算，噪声侧 i.i.d. per-pixel 使 s≥1 的配对噪声独立。若同一"漂移尺度阈值"叙事在空间轴复现，A3 判据就有了可实现的形式；若不复现，说明时间轴现象是光动力学特有。
- **配对定义**：input = 帧 t..t+63 在 (y0,x0) 的 64×128×128 patch；target = **同一帧段**在 (y0+s, x0+s) 的同尺寸窗口（对角平移，与 `_phase_views` 对角约定一致）。s=0 为恒等捷径对照（input=target，量化纯捷径训练的 eval 上限）。协议与 rgrid 完全一致（20ep×100batch、同模型/优化器/损失框架、F1–F3 vs F0 评估 18 patch）。
- **相位视图配对（唯一实现细节变更）**：s 偶数时 out1↔v3、out2↔v4（与时间轴同构）；s 奇数时交换（out1↔v4、out2↔v3）——奇数平移把偶网格结构映射到奇网格，交换后才保持 loss2neighbor 配对相位匹配视图。保证 s 网格内部唯一变量是 s。
- **实现**：`t4b_rgrid.py` 加 `--axis spatial`（数据集加 shift 参数、positions 留 s 边距、同帧配对反而多出 z 余量）；run 目录 `runs/stage01_cidc25_sgrid_seed3407/s{s}`。
- **分析（--analyze；GT 只做测量不更新权重）**：① 实测 PSNR(s) 剂量-响应，s* = argmax；② F0 结构位移 nMSE0(s)=MSE(F0(y),F0(y+s))/Var(F0)（精确可算）；③ **判据可观测性正面回答**：r_noisy(s)=corr(F(y),F(y+s))，i.i.d. 噪声只污染 s=0 一点——空间轴上结构相关性应可直接从噪声数据估计（时间轴 r(d) 平坦的对照面）；④ Spearman(r_noisy, PSNR) 与"平台起点 s ≈ nMSE0(s) 跨 0.02 处"一致性检验。
- **判定标准**：PSNR(s) 随 s 上升后出现平台 且 平台起点与 nMSE0≈0.02 位移尺度重合 → 修正假说空间轴成立，A3 判据 = "估计结构位移/漂移尺度，配对距离取其以上"；PSNR(s) 对 s 平坦或无平台 → H3 空间轴判"不成立"，qmask 混合判据降级为时间轴专用。
- **GPU 预算**：6 变体 × 20ep（rgrid 单变体同协议规模），单卡串行一天内；B2/B3 完成后（~9/16）在 GPU1–3 空卡启动。

### P-B 过平滑与信背比改进（用户观察驱动）

| # | 任务 | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|
| B0 | **平滑基线量化**（零训练）：用 C1 新指标对现有 checkpoint（qmask80ep×3、rgrid 各 d、SN2N/VALID 产出）测 std_ratio/SBR/SSIM/局部方差比，定量确认"过平滑"程度 | C1 | 基线表入本节；过平滑有数字支撑 | ✅（9/9）**全部臂 fg std_ratio<1（0.82–0.95），系统性偏软实锤**；qmask80ep SSIM 0.70–0.73 高于矩阵臂 0.65–0.69；rgrid 内 SSIM 随 d 升（F1 0.68→0.89）但大 d 的 std_ratio 降至 0.75–0.79（大 d 轻度过平滑倾向）；SBR 与 PSNR 不同调（sn2n_xy SBR 0.05 却 PSNR 21.4）→ 多指标必要性获证；**best ckpt 比 final 更锐（std_ratio 0.82–0.88 vs 0.90–0.95）且 PSNR 更高——过平滑不是最优来源，减平滑有望涨点（B1 直接动机）** |
| B1 | **正则降档消融**：~~weight_reg/bg_reg 2×2~~ **修订为 weight_reg 一维扫 {5e-5, 2e-5, 0}**（查证 qmask80ep 配置 `bg_reg_weight` 缺省=0，bg_reg 本就无贡献；对照臂=已有 80ep final run；nf 80ep，1 seed 定方向） | B0 | fg PSNR 不降（seed range 内）+ SBR/std_ratio 改善 + 用户目检 | ⏳→✅训练完成，**待用户定性目检**（9/10，paperviz_b1）。**定量（aligned final，18 patch）**：ctrl(wr1e-4) 22.17/SSIM 0.724/stdR 0.95/SNR 6.24；wr5e-5 21.90/0.800/0.99/4.44；wr2e-5 22.01/0.801/1.00/4.52；wr0 21.36/0.801/1.00/4.88。**读数：降 Hessian 正则使 SSIM +0.08、std_ratio→1.00（过平滑按设计减轻），但 PSNR −0.2~−0.8 dB、SNR 降 ~1.7 dB——"软"是在做去噪功，锐度与去噪存在此消彼长**；wr2e-5 是折中点（PSNR 基本守住 −0.16、SSIM +0.08）。best ckpt 口径同构（wr2e-5 23.15 vs ctrl 23.23）。R10：定稿前需 3 seed。定性图：`runs/paperviz_b1/`（四臂对比 ctrl/wr5e-5/wr2e-5/wr0） |
| B2 | **梯度损失项**：3D Sobel 梯度 L1 加入，小权重扫描 {0.01, 0.1} | B1 | 同 B1 + 用户定性目检更锐 | ⏳ 实现（`training.gradient_loss_weight`，3D 前向差分 L1）+ 冒烟通过；正式两臂运行中（GPU1/2，`stage02_nf_b2_g{001,01}_seed3407`） |
| B3 | **前景加权损失**：按局部能量加权，容量向 fg 倾斜 | B1 | 同 B1，SBR 提升为主验收 | ❌ **fg4 发散，已终止**（9/15 凌晨巡检发现）：ep10 eval fg PSNR 8.26 dB（同类臂同期 22.7–23.2），pred_mean 229 vs target 142（输出整体偏亮 ~+1.4σ，归一化空间同证）；用被中断旧 run 残留 ep60 checkpoint 回测得 fg 9.38 dB 同签名——**系统性发散非暂态，ep1 起即坏**。旧 run（中断前）本就带着这个病跑到 ep61，此前未察觉。机制假说：α=4 加权 MSE 追逐噪声目标视图的前景噪声峰（top-25% 分位在高噪下混入大量正噪声涨落），模型被拉向高噪峰值 → 亮度/对比度整体膨胀（pred_std 73 vs 48 同证）。**待修**：降 α（1.0）或 fg 掩码改用平滑 target 分位、或稳健损失；修复后重跑。GPU3 已释放转投 A2 |
| — | 联动：A 判据选出的大 d 间接抗平滑（恒等捷径=输出输入的轻微平滑版），机制统一叙事入论文 | A1–A3 | — | ⏳ |

### P-C 定量指标扩展（用户约定：PSNR 之外必须看 SSIM/SNR/SBR）

| # | 任务 | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|
| C1 | **扩展 `_region_metrics`**：新增 SSIM（3D per-patch）、SNR（fg mean / bg std）、SBR（fg mean / bg mean）；保留 fg Pearson/std_ratio；单 checkpoint 冒烟 | 无 | 冒烟数字合理；`evaluate_model_on_patches` 默认输出新列 | ✅（9/9）`aces/evaluate_stage2.py`：SSIM 全 patch 统一口径（三 region 行同值）、SNR/SBR 用背景参考（all/foreground 行）；d8@F2 冒烟 2-patch：all SSIM 0.76 / SNR 17.0 / SBR 3.0，fg `pred_std/target_std`=0.83——**从数字上印证用户"偏软"观察** |
| C2 | **历史 run 补列**（零训练，仅重评估）：qmask80ep×3、rgrid d1–d64、矩阵 4 臂×3 seed | C1 | 全部历史 run 的 eval JSON 带 SSIM/SNR/SBR 列 | ✅（9/9，`qmask/backfill_eval_metrics.py`）112 个评估文件全部补列（qmask80ep 10 + 矩阵 48 + rgrid 54，final 覆写原文件、best 另存 `eval_best_metrics*`）。**两点方法学记录**：①原官方 eval 是 **final 权重**口径（best 单独保存）；②重评估有推理侧 cudnn 非确定性（fg PSNR ±0.3 dB，如 21.84→22.17），**官方表须以补列批次文件为准整套重新生成，不可新旧混排**。汇总：`runs/metrics_backfill_summary.json` |
| C3 | **常规化**：paperviz/报告表默认含新列；本约定记入 §8 小结 | C1 | — | ✅（9/14，`qmask/paperviz.py`）新常量 `METRIC_SPECS`（PSNR-all/fg/bg、SSIM、SNR-fg、SBR-fg、stdR-fg= pred_std/target_std 现算）统一三处渲染：①`render_summary_table` 动态列+单样本纯均值+**PIL 限宽 1200px**（CLAUDE.md 读图约束）；②`render_metric_bars` 改 1×N 指标子图、yerr=seed 半极差；③`--arm` 模式新增定量表 `metrics_arms.png`（eval_metrics_aligned.json 优先、final 兜底，缺文件的臂跳过并告警）。冒烟三路渲染全通过（矩阵 4臂×3seed：qmask 21.39/0.716/0.989 vs valid 20.60——与 C2 补列批次一致） |

**执行顺序**：A1 ∥ C1（立即，零 GPU）→ C2（GPU 便宜，评估 only）→ B0 → A2、B1（GPU 并行）→ B2/B3 → A3 → A4（R10 收尾）。

---

## 8. 修改小结（逐次追加，每次修改必记）

### 2026-09-04 #1：E1 管线用错（2D/3D）——3P 定性 FAIL，两 leg 重跑
- **触发**：用户肉眼复核 review 切块：nf 基本通过，**3P 去噪明显不对，判 FAIL**（推翻同日数值代理审计的"有条件通过"结论——数值审计仍保留作为辅助证据，但定性裁决以人工目检为准）。
- **根因**：原 SN2N 仓库有 2D/3D 两套管线，E1 两条 leg 均误用 3D 管线。nf（2D+time）应走 2D 管线；3P（3D）的 `P2Pmode=1`（t=z 轴互换）与本项目归因结论 N_repeat(z,1)≈1 冲突（z 向配对无独立噪声实现→过度平滑），应改 `P2Pmode=2`（单帧内 xy 配对）。代码证据：`Predictor2D` 逐帧 normalize（与 datagen per-file min-max 一致）；`Predictor3D` 裸 astype('uint8')。
- **改动**：新增 `ACES/logs/run_e1_nf_2d.sh`（GPU1）、`ACES/logs/run_e1_3p_p2p2.sh`（GPU3，单变量 P2Pmode 1→2）；新增 `ACES/logs/render_e1_viz.py`（外部基线可视化统一为 qmask.visualize 同款方案：magma + `_robust_lims`(0.5,99.5) + 同款 quarter-offset 256² crop）。
- **决定**：外部基线可视化必须与五档归因实验同一渲染方案（用户要求，固化）；原 3D-P2Pmode1 两 leg 数字（3P 16.93 / nf 15.53）保留作对照，不入外部表；重跑结果出来后重做"定性→定量"验收。
- **工具事实**：本会话多模态通道失效（Read 返 base64 非视觉内容），定性目检由用户承担；数值代理审计（`audit_e1_numeric.py`）留作常规自检。

### 2026-09-04 #2：nf-2D epochs 4→9（步数预算对齐）
- datagen 实测 312,908 patch → 9,778 batch/epoch（bs=32），首轮 epochs=4 总步数仅 3.9 万，不足 ≈89k 约定（3P-100ep 步数预算，nf-3D 同例）。按脚本内既定约定上调 epochs=9（9,778×9≈8.8 万步），终止首轮重启训练（datagen 缓存复用，损失 ~35 分钟）。`run_e1_nf_2d.sh` 已改，注释同步。
- E2-nf（VALID 原版，GPU2）于分类器恢复窗口启动成功：135 batch/epoch，loss 0.4–3.2 正常。

### 2026-09-04 #3：E1-nf-2D 完成（eval + 审计 + 统一可视化）
- `run_e1_nf_2d.sh` 全流程完成（9ep 训练 ~4.5h + Predictor2D 推理）。统一尺 eval（`eval_e1_nf_2d.py`,与 3D leg step3c 同协议）:**fg 13.71 / all 13.28 / bg 13.18 dB**,全局对齐 2.90·pred+86.06。
- 数值审计 PASS(无塌缩/伪影:std 比 0.41、fg Pearson 0.33、局部方差相关 0.67、饱和 0.66%、SBR 保持 102%)。
- ⚠️ 关键读数:**2D 管线统一尺 fg 反而低于 3D 管线(13.71 vs 15.53)**——"数据类型匹配管线"并未自动带来更好数字;nf 3D 管线(t 轴配对)虽然"几何不符"却更高。待用户目检 2D 输出定性后再定外部表 nf 行取哪条(两条都是原仓库管线,协议上 2D 更"对应";R10 单次运行仅供定性)。
- 统一可视化已出:`Results/sn2n_original_neurofinder_2d/figures/qualitative_comparison_ext.png`(magma+robust_lims 同款方案)。
- 进度:3P-p2p2 epoch 61/100(剩 ~3.7h+推理);E2-nf epoch 50/100(tqdm 剩 ~3.9h)。

### 2026-09-05 #4：E1-3P-p2p2 完成——配对几何修复成功（fg 16.93→18.40）
- **推理事故与修复**：`run_e1_3p_p2p2.sh` 首跑误将 uint16 raw_data 直喂 Predictor3D（其契约为裸 `astype('uint8')`，mod-256 回绕，预测与 target Pearson≈0，作废删除）。已按原 3P leg 流程重建 raw_data_8bit（p0.1–p99.9 归一化）重跑推理（脚本已修正注释）。注意 Predictor2D 无此问题（逐帧 normalize），nf-2D 不受影响。
- **统一尺 eval**（`aces/eval_sn2n_original_3p_p2p2.py`，E2 同款 ReadDatasets 网格 + top-18 fg patch）:**fg 18.40 / all 19.67 / bg 15.12 dB**,全局对齐 172.87·pred−8.19。对比:P2Pmode=1 原跑 fg 16.93 → **+1.47 dB**;原 VALID 18.53——**SN2N-p2p2 与原 VALID 在 3P 上基本持平**。
- **数值审计 PASS 且结构性大幅改善**:fg Pearson 0.22→**0.62**,std 比 0.48→0.66,无饱和,局部方差相关 0.12(正但偏弱),SBR 保持 64%。
- **结论**:用户"用对应管线/正确几何"的判断被定量证实——3P 上的失败根因是 P2Pmode=1 的 z 轴互换配对,与本项目 N_repeat(z,1)≈1 归因一致。待用户目检 `Results/sn2n_original_3p_p2p2/figures/qualitative_comparison_ext.png` 后,外部表 3P 行采用 SN2N-3D-p2p2。
- 待办:E2-nf(VALID)还在跑;nf 外部表行等用户裁决(2D 主行 vs 3D 脚注,见小结 #3)。

### 2026-09-05 #5：E2-nf 完成——外部表四行齐,附"时间上下文优势"协议注记
- `run_e2_valid_nf.sh` 全流程完成(100ep, 13703 iteration, epoch_100 ckpt)。统一 eval(`aces/eval_valid_original_nf.py`,与 E1-nf 同尺:stride-16 网格 PZ64/PW128/PH128 + ROI mask + per-patch 分位 data_range,无对齐步):**fg 23.97 / all 24.53 / bg 24.67 dB**。
- 数值审计(`e2nf_fullstack_audit.py` 全栈 tiled 推理 + 审计):PASS 且近乎复现 target——fg Pearson **0.91**、std 比 1.04、局部方差相关 0.96、SBR 保持 99.99%。
- ⚠️ **协议注记(解读时必须带上)**:nf 的 z 轴是时间。VALID 的 3D N2N 以 z 邻域为配对噪声实现,输入 224³ patch 含 224 个连续时间帧——网络可对时间上下文做内部平均,而 proxy target 恰是全栈时间均值,天然奖励"上下文平均"型方法。23.97 dB 是真实读数(非泄漏),但部分来自该结构性优势;SN2N-3D 在 nf 上只看 16 帧上下文,SN2N-2D 只看单帧。跨方法比较时此点必须写明。
- **外部表四行齐**:3P:VALID 18.53 vs SN2N-3D-p2p2 18.40(对照 p2p1 16.93);nf:VALID 23.97 vs SN2N-3D-p2p1 15.53 / SN2N-2D 13.71(nf 行取哪条待用户裁决,小结 #3)。
- E2-nf 定性图:`Results/valid_original_neurofinder/figures/qualitative_comparison_ext.png`。

### 2026-09-05 #6：用户定性裁决——3P SN2N 两版均未通过；nf 行可定稿；转入 qmask 创新优化
- **用户目检裁决**（9/5）：①3P-p2p2 **定性不通过**（"与 target 差距极大"）——⚠️ 重要方法论事实：其 18-patch fg 18.40 dB（与 VALID 18.53 持平）**与目检矛盾**,说明 per-patch PSNR 在 fg 富集 patch 上对"亮斑对齐但结构/对比错误"不敏感（全局线性对齐 + p0.1–p99.9 data_range 可能掩盖感知差异）,**patch PSNR 不能单独作为外部基线的验收依据,定性目检一票否决**;②E2-nf（VALID 原版,23.97 dB）通过;③nf-2D（13.71）虽逊于 E2-nf 但可接受。
- **外部表定稿状态**：3P 行——VALID 18.53 保留;SN2N 行**暂缺**（p2p1/p2p2 均定性未通过,数字留作对照,表中标注"定性未通过"）;nf 行——VALID 23.97（含时间上下文注记）,SN2N 行取 **2D 管线 13.71 主行 + 3D-p2p1 15.53 脚注**（按既定建议"管线-数据对应优先"）。
- **SN2N-3P 后续**（低优先,不阻塞创新线）：如需补行,候选方向=延长训练/更大 P2Pup/检查 8-bit 输出量程压缩（172× 拉伸暗示输出对比度坍缩,或为感知差异主因）。
- **决定**：按用户指示转入 **qmask 方法的继续创新优化**（见 §4 新增 item 10 路线）。

### 2026-09-05 #7：重复矩阵(4 臂×3 种子)启动——item 10(a)
- 配置:`/data3/.../ACES/configs/stage02_nf_matrix4_seed{3407,3408,3409}.yaml`——4 臂 [valid, sn2n_xy, iso3d, qmask],nf 数据集,预算 80ep×100batch=8k updates/run(介于 3ep pilot 与 80ep 全协议之间,记录为矩阵专用协议),评估 patch_count=18 与外部 nf 行同尺。
- qmask 臂 = **Q-2 修订版**(geometry.n_decision=false,proxy 分支 S+Q 主导,τ 默认 0.95/0.85/0.60);stage1 复用 seed3407 pairability(几何决策与训练种子解耦)。
- 调度:`run_nf_matrix.sh <gpu>`——GPU1=valid×3+iso3d¹,GPU2=sn2n_xy×3+iso3d¹,GPU3=qmask×3+iso3d¹;日志 `/data3/.../ACES/logs/nf_matrix/`。预计 ~4-5h。
- 产出将裁决:①R9 端点赤字真伪(valid vs qmask 的 mean±范围重叠检验);②Q-2 修订对 qmask 臂的影响;③scale-up 配置选择(item 7)。

### 2026-09-06 #8：重复矩阵完成——R9 赤字不复现；种子方差≈臂间差；scale-up 定 iso3d+qmask
- **12/12 全部完成**（80ep×8k updates,18-patch fg dB,eval_mode=full_resolution,与外部 nf 行同尺）：

  | 臂 | fg mean±std | fg 范围 | bg mean | fg std_ratio(塌缩检查) |
  |---|---|---|---|---|
  | iso3d | **22.14±0.43** | [21.65, 22.44] | 29.64 | 1.08 |
  | qmask(Q-2修订) | 21.96±0.75 | [21.50, 22.82] | 29.63 | 1.08 |
  | valid | 21.31±1.08 | [20.07, 22.01] | 29.29 | 1.09 |
  | sn2n_xy | 21.14±1.06 | [19.98, 22.07] | 29.10 | 1.06 |

- **裁决①（R9）**：**端点赤字不复现**。逐种子配对差 qmask−valid = [−0.28, +2.75, −0.51]（均值 +0.65 但符号不一）;qmask mean 反超 valid 0.65 dB。此前的 −1.49 dB 单次赤字属 R10 记录的种子噪声（本次 valid 臂内范围即达 1.94 dB）。R9 降级为"已裁决:无系统性端点赤字"。
- **裁决②（Q-2）**：n_decision=false 的 qmask 臂健康——无塌缩（std_ratio 1.08）、bg 正常（29.63）、mean 位居第二且单种子最高（22.82）。修订版可作默认;真 Q-2 消融（true vs false 同预算×3 种子）留作后续可选项。
- **裁决③（scale-up 配置,item 7/10c）**：iso3d 与 qmask 领先且显著稳于 valid/sn2n_xy（std 0.43/0.75 vs 1.06/1.08）。**scale-up 选 iso3d（对照）+ qmask（主方法）双臂**;n=3 下任何 <1 dB 的臂间差不作结论,后续创新效果判定一律 ≥3 种子报 mean±std。
- 局限注记:矩阵预算 8k updates 低于外部 E2-nf 全协议(100ep/13.7k steps,fg 23.97);臂间排序在更大预算下未必保持,scale-up 后复核。

### 2026-09-06 #8：重复矩阵完成——R9 赤字不复现,Q-2 自洽性证实,scale-up 放行
- **12/12 run 全部完成**(harness 会话重启标记 stopped,训练进程实际跑完,以落盘 eval_metrics.json 为准)。结果(fg PSNR mean±半程,内部表,R7 不与外部混排;汇总 `runs/nf_matrix4_summary.json`):
  - valid 21.31±0.97(all 29.17±0.55) | sn2n_xy 21.14±1.05 | iso3d **22.14±0.40** | qmask(Q-2) **21.96±0.66**
- **裁决① R9 端点赤字:不复现,解除**。qmask(Q-2) fg 高于 valid +0.65 dB,范围大幅重叠;且 valid 单臂种子跨度 1.94 dB(20.07–22.01),证实 R10 的 1.5 dB 单次差是常态噪声——原 −1.49 dB 赤字属于 3ep pilot 规模的单次方差,非实现缺陷。R9 附加解除条件:结论仅适用于 ≥8k updates 预算 + Q-2 修订版。
- **裁决② Q-2 自洽性:证实**。Q-2 单元测试预测 nf 上 n_decision=false → 111 全开;矩阵实测 qmask(Q-2) ≈ iso3d(全开各向同性,差 0.18 dB,范围重叠)——数据驱动决策与几何退化完全一致。(Q-2 的独立因果贡献需 true/false 双臂对照矩阵,列为后续可选项,不阻塞。)
- **裁决③ scale-up:放行,配置=qmask(Q-2) 全预算 80ep**(66.5k updates,`max_batches=-1`,832 patch/epoch)×3 种子;理由:与 iso3d 打平且数据驱动、自洽,且 fg 种子稳定性(±0.66)优于 valid(±0.97)/sn2n_xy(±1.05)。
- **方法层洞察(创新线输入)**:iso3d/qmask 的 fg 种子稳定性(±0.40/±0.66)显著高于 valid/sn2n_xy(±0.97/±1.05)——采样几何各向同性化降低训练方差,这本身可作为 qmask 的一个可主张优势("data-driven 几何选择在准确率打平的同时降低方差")。

### 2026-09-06 #10：可视化流程固定化——新增 `qmask/paperviz.py`,所有实验统一复用
- 按用户指示,把 Results/neurofinder_00_00_multimethod_comparison_20260822 的可视化代码规范提炼为固定模块 `ACES/qmask/paperviz.py`,今后每次实验产出后直接复用,不再临时重写脚本。
- **固定约定**(源自参考脚本):colormap=turbo;overview 共享窗宽 (1.0, 99.85) 百分位、ROI zoom 逐 crop (1.0, 99.9);固定 METHOD_ORDER=[noisy,valid,sn2n_xy,iso3d,qmask,target] 与 LABELS/COLORS;条形图带数值标注+网格;rcParams font.size=9、去 spines、dpi=150;ax.table 汇总表;**每张大图落盘后立即切 ≤1200px review 行块**(复用 preview 的 `_save_bounded_png`),遵守 CLAUDE.md 大图禁读规则。
- **两种入口**:
  - `--run-dir <run>`:单 run 定性图(overview MIP+ROI 框、ROI 放大+target 前景红轮廓、eval scored patch 行图)+定量条形图+汇总表;
  - `--matrix-root <runs> --seeds 3407,3408,3409`:矩阵汇总图(3 panel all/fg/bg PSNR mean±半程条形 + ax.table)。
- 模型加载契约沿用 `_best.pth`→`_final.pth` 回退;推理 `_tiled_predict`(64×128×128 tile,逐 stack mean/std 归一→反归一);patch 索引直接复用 `eval_metrics.json` 的 `selected_patch_indices`——**可视化 patch = 被评分 patch**,图文严格对应。
- 目标 patch 索引约定:ReadDatasets `__getitem__` 返回 (…,z_idx,w_idx,h_idx),`[3]`=y、`[4]`=x(曾写反,已修正)。
- 产出目录默认 `<run>/paperviz/` 或 `<matrix_root>/paperviz/`,内含 review tiles + index.html。
- 用法:`PYTHONPATH=ACES CUDA_VISIBLE_DEVICES=0 python -m qmask.paperviz --matrix-root /data3/.../ACES/runs --seeds 3407,3408,3409`

### 2026-09-06 #11：T4b 切片 2 启动——`qmask/t4b_rgrid.py`,时间配对距离 d 网格(H3 闭环)
- **新增模块 `ACES/qmask/t4b_rgrid.py`**(零外部源修改,复用 `_load_valid_components`/`evaluate_model_on_patches`/qmask 损失)。
- **设计**(使用条款:Validation 集只做评估/模型选择,权重更新仅用 Training 集):变体 = 时间配对距离 d∈{1,2,3,4,6,8}。input=帧 t..t+63 的 y/x 对角相位双视图 (0,0)/(1,1),target=帧 t+d..t+d+63 同相位双视图——input/target 内容差=恰好 d 帧时间漂移,损失与矩阵各臂完全一致,d 网格内部唯一变量是 d。训练数据 = Training 4 栈混合(A1/B1/C2/D2,per-stack mean/std)。
- **评估**:每 d 模型在 F1/F2/F3 上 18-patch 评估(矩阵同规格),target=真值 F0;mid-eval(F2,12 patch,每 5 ep)选 best-F2 权重;final 与 best 两套权重均评三档。
- **闭环分析 `--analyze`**:Q_t(d)(第一切片曲线,fg,τ_s=0.85)与实测 PSNR(d) 的 Spearman(proxy/gt 双口径)+ S_measurable 口径;预测 d_max=max{d:S_measurable≥τ_s} vs 实测 argmax d*;产出 `metrics/h3_closure.json`。
- **规模**:pilot 20ep×100batch×1 seed,6 run 并行(GPU1:d1,d2 / GPU2:d3,d4 / GPU3:d6,d8),与 qmask80ep 共卡(~25 GB/48 GB)。~1 min/epoch(栈 memmap+page cache 后明显快于矩阵 run)。R10:最终 H3 主张前需 ≥3 seed 重复,pilot 通过后补。
- 配置:`/data3/.../configs/t4b_cidc25_rgrid_seed3407.yaml`;run:`runs/stage01_cidc25_rgrid_seed3407/d{d}/`;日志:`logs/t4b_rgrid/rgrid_d{d}_seed3407.log`。

### 2026-09-06 #12：T4b 切片 2 完成——H3（pairability 预测最优采样几何）在 CIDC25 上被证伪
- **运行**：9 个时间配对距离变体 d∈{1,2,3,4,6,8,16,32,64}（第二批 d16/d32/d64 + d8-seed3408 复证），20ep×100batch，`runs/stage01_cidc25_rgrid_seed3407/d{d}/`；d8 另有 seed3408 复证 run。产物 `metrics/distance_summary.json` + `metrics/h3_closure.json`。
- **定量结果（fg PSNR，18 patch，vs 真值 F0，best-F2 权重）**：
  - PSNR(d) 陡升 d1→d8 后**平台**：d8–d64 各档差距 ~1 dB 内，且平台形状**噪声级无关**（F1/F2/F3 同构）。
  - 实测最优 d*：F1 **d16=28.35**、F2 **d8=25.63**、F3 **d64=22.89**（F3 上 d64 仅比 d8 高 ~0.75 dB）。
  - d8 seed3408 复证：F1 29.36 / F2 25.48 / F3 22.44（与 seed3407 差 ±0.08/±0.05 dB 级）——d8 的高价值非种子噪声；weight_set=final 复现同构。
  - **Spearman(Q_t(d), PSNR(d)) ≈ −1**（proxy 与 gt 双口径一致，反相关）；τ_s 门控预测 d_max=8/2/0（F1→F3），实测 16/8/64——**预测方向性错误，F3 最严重**（门控判定"无合法时间配对"，实测大 d 增益最大，+10 dB 级 vs d1）。
- **定性验收**：`figures/rgrid_qualitative_F3.png`（2234×1405，3 行×[F3 noisy | d1 | d8 | d64 | GT F0]），review 切块三行全部读毕——d1 面板明显欠去噪（贴近 noisy 输入），d8/d64 结构接近 GT，无塌缩、无伪影；与"大 d 强制真去噪"一致。
- **结论：H3 作为实例化版本被证伪**（当前 qmask 几何判据——结构相关性 S/τ_s 门控——不能预测最优时间配对距离，甚至反预测）。
- **机制解读**：CIDC25 为仿真逐像素 i.i.d. 噪声，任意 d 均去相关；小 d 时 target≈input，恒等/平均捷径损失最小，模型偷懒；大 d 惩罚捷径、强制真去噪。方向与"结构相关性决定配对距离"理论相反。**局限**：仿真 i.i.d. 噪声（无结构化噪声）；仅测时间轴（空间轴未测）；20ep pilot 规模。
- **对 qmask 主线的影响**：几何决策判据（Q-2 的 S+Q 主导方向）需再校准——从"选结构相关性最高的配对"转向"回避恒等捷径"（如 d_min 下限由 shortcut 风险而非 N/Q 阈值决定）；入 §4-10(c) 候选。
- **待办（R10）**：论文级 H3 主张前补 ≥3 seed 全网格重复；可选空间轴（y/x 位移网格）闭环验证。

### 2026-09-06 #13：新增 §7 优化推进 Plan——判据重构(P-A)/抗平滑(P-B)/指标扩展(P-C)三条线
- **输入**：H3 证伪（#12）+ qmask80ep 三 seed 完成（fg **22.14±0.33**，全场最优臂；iso3d 20.92 / qmask 21.01 / valid 20.46 / sn2n_xy 20.23，矩阵内 qmask vs valid +0.55 dB 仍被 ±0.8 range 淹没）+ 用户两点指示：①去噪结果偏软、平滑过多，SBR 待提升；②定量分析今后必须 PSNR+SSIM+SNR+SBR 全面报告（已接受为约定）。
- **结构**：§7 为三线 Plan 表（P-A 几何判据 shortcut-risk 重构 + 空间轴闭环 + R10；P-B 平滑基线量化→正则降档→梯度损失→前景加权；P-C 指标扩展→历史 run 补列→常规化），原 §7 修改小结顺延为 §8。
- **执行顺序**：A1∥C1（零 GPU，立即）→ C2（评估 only）→ B0 → A2∥B1（GPU）→ B2/B3 → A3 → A4。

### 2026-09-09 #14：A1+C1 完成——shortcut-risk 判据被否定，平台起点 d=8 是唯一档内不变量；评估新增 SSIM/SNR/SBR
- **A1（`qmask/shortcut_risk.py`，产物 `runs/stage01_cidc25_rgrid_seed3407/metrics/shortcut_risk_analysis.json`）**：纯噪声可观测 r(d)=corr(F_t,F_{t+d})、nMSE(d)、捷径拉力 pull=nMSE_id−noise_frac 与实测 PSNR(d) 对撞。**否定性结论**：三个观测量在档内对 d 都近乎平坦，档内 Spearman(r,PSNR)=−0.82/−0.87/−0.85（与 Q 同向反预测）——"选 r 低于阈值的最小 d"这类 shortcut-risk 判据**同样不成立**。保留的实证不变量：①**平台起点三档恒为 d=8**，与 F0 结构漂移 nMSE0 在 d8 跨越 ~0.02 重合（结构漂移尺度噪声无关）；②跨档 pull 量级 0.58/0.90/0.97 与大 d 增益 +3.4/+7.6/+9.6 dB 正相关。**修正假说**：小 d 失败是优化动力学问题（梯度信号弱、恒等解是强吸引子），非静态损失差——A2/A3 判据按此修订；真数据上结构漂移不可直接观测（高噪下被淹没），判据可观测性是 A2 要正面回答的问题。
- **C1（`aces/evaluate_stage2.py`）**：`_region_metrics` 新增 SSIM（全 patch 统一 data_range，三 region 行同值）、SNR=20lg(fg_mean/bg_std)、SBR=20lg(fg_mean/bg_mean)；`evaluate_model_on_patches` 汇总列同步扩展；NaN 聚合不再触发 warning。冒烟（d8@F2，2 patch）通过且冒烟产物已删（新约定：冒烟通过后开正式实验时顺手删冒烟目录）。数字：all SSIM 0.76/SNR 17.0/SBR 3.0；fg pred_std/target_std=0.83 量化印证"偏软"。
- **待办**：C2 历史 run 补列（评估 only）→ B0 平滑基线表 → A2 空间轴闭环（判据可观测性问题）∥ B1 正则降档。

### 2026-09-09 #15：C2 补列完成（112 文件）+ B0 平滑基线表 + B1 三臂启动
- **C2**：`qmask/backfill_eval_metrics.py` 三族补列（qmask80ep×3、矩阵 4臂×3seed、rgrid 9d×2权重×3档），final 覆写原文件、best 另存 `eval_best_metrics(.aligned).json`。**方法学两点**：①原官方 eval = final 权重（此前未记录，best 曾被误认为官方口径）；②推理 cudnn 非确定性使重评 fg PSNR 漂移 ±0.3 dB（21.84→22.17）——官方表今后以补列批次文件整套生成。插曲：seed3407 qmask 官方 aligned 文件曾被 C1 探针以 best 权重覆盖，已删除并用 final 口径补回。冒烟/探针产物全部清理。
- **B0**（`runs/metrics_backfill_summary.json`）：全部臂 fg `pred_std/target_std` 0.82–0.95（系统性偏软实锤）；qmask80ep SSIM 0.70–0.73 > 矩阵臂 0.65–0.69；rgrid 大 d 的 std_ratio 降至 0.75–0.79；SBR 与 PSNR 不同调；**best ckpt 比 final 更锐且 PSNR 更高 → 减平滑有望涨点**。
- **B1 启动**：weight_reg ∈ {5e-5, 2e-5, 0}（原 2×2 修订：bg_reg_weight 缺省 0 无贡献），对照=已有 80ep final run，seed3407，GPU1/2/3，~2.5 天。
- **下一步**：B1 出数前推进 A2 设计（判据可观测性）；B1 完成后先定性（review 切块）再定量（新指标全套）。

### 2026-09-10 #16：B1 三臂完成——降 Hessian 正则提升 SSIM/std_ratio 但牺牲 PSNR/SNR；paperviz 支持 --arm 跨 run 对比
- **B1 定量**（aligned final）：ctrl(wr1e-4) 22.17/SSIM 0.724/stdR 0.95/SNR 6.24/SBR 0.17；wr5e-5 21.90/0.800/0.99/4.44/−1.00；wr2e-5 22.01/0.801/1.00/4.52/−1.01；wr0 21.36/0.801/1.00/4.88/−0.96。best 口径同构（wr2e-5 23.15 vs ctrl 23.23）。**结论：Hessian 正则的"软"承担了去噪功能——降正则换结构保真（SSIM +0.08）与锐度（stdR→1.0），代价是残留噪声（SNR −1.7 dB）与 PSNR 下降；wr2e-5 为当前最优折中**。待用户定性目检（伪影/噪声残留是主要风险项）；R10 定稿前补 3 seed。
- **paperviz 增强**：新增 `--arm label=mode_dir`（可多次），支持跨 run 目录的单臂对比定性（此前写死四臂同目录布局）；ruler patch 坐标取首个 arm 的 eval_metrics.json。B1 四臂图：`runs/paperviz_b1/`（index.html + review 切块）。
- 会话备注：模型切换 GLM-5.3 无多模态读图——定性验收由用户人工完成，模型侧只做定量。

### 2026-09-10 #17：B1 定性验收通过（用户目检：四臂视觉差别不大，无伪影/噪声残留）；启动 B2/B3
- **用户判定**：ctrl/wr5e-5/wr2e-5/wr0 视觉差别不大 → 降正则未引入伪影或残留噪声，B1 通过；数字上 wr2e-5 仍为折中最优（SSIM +0.08 / PSNR −0.16）。R10 定稿前补 3 seed。
- **B2/B3 启动**（P-B 剩余两项，均在 qmask/train.py 加 config 开关、默认关闭零行为变化）：
  - **B2 梯度损失**：`training.gradient_loss_weight` ∈ {0.01, 0.1}，3D 前向差分 L1(out,tgt) 对齐锐度；
  - **B3 前景加权**：`training.fg_loss_weight`（4.0，percentile 75），loss2neighbor 的 fg 体素加权，容量向 fg 倾斜提 SBR。

### 2026-09-14 #18：B2/B3 中断重启——会话退出连带杀死后台训练；改用 setsid 脱离会话
- **事故**：上一 Claude Code 会话退出时，其后台 shell 的子进程（3 个 qmask.train）被连带终止——b2_g001 死于 ep61/80、b2_g01 死于 ep58/80、b3_fg4 死于 ep61/80，无 qmask_final/metrics。qmask.train 无断点续训能力（checkpoint 不含优化器状态），且 ep60 续训与对照 80ep 连续训练协议不一致（Adam 重热身），故从零重跑。
- **防复发**：改用 `setsid nohup ... &` 启动，训练进程脱离会话（ps 确认 PIDs 246301/246302/246303 存活，GPU1/2/3）。此问题只影响 Claude Code 工具后台任务，不影响此前 nohup/直接终端启动的 run。
- **残留清理**：中断 run 的旧 checkpoint（epoch0010–0060/best）留在原目录；已确认 train.py 的 best_psnr 在进程内存中从 −inf 起步、eval_curve.csv 每次全量重写（"w" 模式），新进程 ep10 首次 eval 即覆盖 best.pth 与曲线，脏状态自愈。

### 2026-09-14 #19：C3 完成——paperviz 全指标列常规化（表格/柱图/跨 run 表），顺带 1200px 限宽
- **改动**（`qmask/paperviz.py`）：新增 `METRIC_SPECS`（PSNR-all/PSNR-fg/PSNR-bg/SSIM/SNR-fg/SBR-fg/stdR-fg，stdR= pred_std/target_std 现算，NaN 容错），`collect_arm_metrics`/`collect_matrix_metrics` 改为全指标结构；`render_summary_table` 动态列、"—"表全缺、单样本出纯均值；`render_metric_bars` 改为 1×N 指标子图（yerr=seed 半极差）；`--arm` 模式新增 `metrics_arms.png` 跨 run 定量表（aligned 优先 final 兜底）。表格 PNG 落盘后 PIL 限宽 1200px（遵守 CLAUDE.md 读图约束）。
- **冒烟**：/tmp/c3_smoke 三路渲染全通过，矩阵均值与 C2 补列批次一致（qmask PSNR-fg 21.39 / SSIM 0.716 / stdR 0.989）。遵守冒烟清理约定：正式使用即删 /tmp/c3_smoke。
- **后续影响**：B2/B3 与 A4 验收直接复用该代码出表，无需再手写对比脚本。

### 2026-09-14 #20：A2 空间轴闭环设计定稿（零 GPU 部分）——配对定义、奇偶相位交换规则、判定标准
- **设计要点**（全文见 §7 P-A 设计块）：input/target = 同一帧段 (y0,x0) vs (y0+s,x0+s) 对角平移双窗；s=0 恒等捷径对照；协议同 rgrid（20ep×100batch）。唯一损失结构变更是奇数 s 时相位视图配对交换（out1↔v4/out2↔v3），保证配对始终相位匹配、网格内唯一变量是 s。
- **A1 修正假说的对撞设计**：空间轴把"漂移"变成可精确计算的几何位移 nMSE0(s)，且 i.i.d. 噪声下 r_noisy(s)（s≥1）可直接从噪声数据估计——正面回答 A1 留下的"判据可观测性"问题；判定标准=平台起点与 nMSE0≈0.02 是否重合。
- **状态**：实现（`t4b_rgrid.py --axis spatial`）与训练待 B2/B3 释放 GPU1–3 后启动；A2 设计先行不改执行顺序（A3 仍排在 A2 出数之后）。

### 2026-09-15 #21：B3(fg4) 系统性发散确认并终止——巡检发现 + 旧 ckpt 回测定案；GPU3 转投 A2
- **发现**（01:03 排程巡检）：b3_fg4 重启后 ep10 eval fg PSNR 8.26 dB（b2 两臂同期 23.20/22.73，B1 三臂 22.9–23.0）；pred_mean 229 vs target_mean 142、pred_std 73 vs 48——输出整体偏亮且对比膨胀，SSIM 0.715 尚可（结构对、强度错）。
- **定案**：用被中断旧 run 残留的 qmask_epoch0060.pth（mtime 09-13 01:01，重启前）复刻 mid_eval 口径回测：fg 9.38 dB、pred_mean 210——旧 run ep60 与新 run ep10 同一发散签名 → **非暂态、非 cudnn 噪声，α=4 的 fg 加权损失从训练早期就系统性发散**；旧 run 中断前已带病跑 61ep 而未察觉（当时只检查了进程与 checkpoint 存在性）。ep1 冒烟只能验证管线通，验不出这种需要数 ep 才显形的发散——**后续新损失项冒烟需加"ep10 读数与对照臂同期 ±2 dB"哨兵线**。
- **动作**：kill 246303 终止 b3（避免再烧 ~2.4 天 GPU3）；b2 两臂不受影响。发散 run 的 checkpoints/eval 产物留在原目录作诊断样本，不清理。
- **机制假说**（待修复实验检验）：α=4 加权 MSE 上，fg 掩码取噪声 target 视图 top-25% 分位——高噪下这些体素混入大量正噪声涨落，5× 权重使模型追逐噪声峰 → 输出亮度/对比度膨胀。修复方向：α 降至 1.0；或掩码改用平滑后 target 的分位；或对加权项做均值归一（Σw/N）保持损失量级。
- **顺带**：GPU3 空出，A2 sgrid 六变体提前启动（原排在 B2/B3 全部完成后）。

### 2026-09-15 #22：A2 sgrid pilot 出数——空间轴无时间轴式平台，位移收益由噪声级决定；平移混淆证伪
- **执行**：6 变体（s∈{0,1,2,4,8,16}）在 screen `a2_sgrid`（GPU3）串行 ~35min/个全部正常完成，`--analyze` 闭环出数（`runs/stage01_cidc25_sgrid_seed3407/metrics/sgrid_closure.json`）。
- **混淆排查**：①"模型学会平移输出 s 像素导致评估错位"假说用**平移补偿 GT**（F0[:, s:, s:]）评估证伪——s=2/8 补偿前后 PSNR/SSIM 不变（17.41/17.41、18.07/18.01）；②训练半分辨率相位视图 vs 评估全分辨率 patch 的口径与 rgrid 完全一致（rgrid 同口径 SSIM 0.76），非病因。
- **核心读数**（eval_best，fg PSNR dB）：F1 s0=24.5→s16=17.9（位移无收益，s0 即最优）；F2 s0=17.8→**s1=20.4**（峰）→s16=18.2；F3 s0=12.5→s8/16=17.4/18.1（**类时间轴剂量-响应，+5.6 dB**）。
- **解读**：①空间位移训练的收益 = 恒等捷径危害程度的函数（随噪声级 F3>F2>F1），支持 A1 修正假说的优化动力学解读；②**"平台起点 ↔ nMSE0 跨 0.02"的跨档不变量不向空间轴迁移**（阈值 s=1 仅与 F2 平台重合，F1 最优 s=0、F3 最优 s=16）；③r_noisy(s)（i.i.d. 噪声下空间自相关可观测，A1 缺的对照面）Spearman 跨档反号（+0.94/−0.09/−0.83）——空间可观测量同样不是稳定判据。**H3 空间轴判定：统一几何判据不成立**，qmask 无需"空间用 Q"混合判据；几何选择的价值随噪声级（捷径危害度）变化这一结论进论文叙事。
- **局限**：20ep×100batch pilot、1 seed；绝对值远低于 80ep 生产 run，仅网格内比较有效。R10：sgrid 3 seed 待补（与 rgrid 3-seed 同批排队）。

### 2026-09-15 #23：新增整体验收文档 project_progress_status.md
- 应用户要求（计划逐次记录详细但整体验收不清晰），新增同级文档 `Denoising_PM/project_progress_status.md`：开头即主线 + 三个创新点（IC1 逐轴几何/IC2 共位相位分裂/IC3 H3 可证伪检验）及其验证状态，随后阶段完成总表（结论一句话 + 证据目录）、计划修订轨迹（~~原文~~→**修订** 对照，8 条）、待办风险。本文档头部已加指引；后续每个重要节点同步更新该文档。

### 2026-09-15 #24：项目快照推送 GitHub（GPT 审阅用）
- **仓库**：https://github.com/winb0408/Denoising_PM（main，commit 3bc7cce）。暂存区 `/data3/wjb/Denoising_PM/github_stage/Denoising_PM`：data2（代码+文档）与 data3（runs/logs/configs）同名目录合并；**排除** Methods/References/Results/原始数据/全部 checkpoint/*.pth/*.tif/paperviz 图像，另删 sampler_metadata.json（单文件 67M）与 >3M train_losses.csv；含 ACES 全部代码、9 个根级 md、610 个文件、约 30M。
- **配套**：仓库根 README.md 面向审阅者（主线/三创新点状态/代码地图/证据地图/阅读顺序/条款约束/诚实遗留清单）。
- **插曲**：/data2/wjb 本身是外层 git 仓库，`git add -A` 曾误在外层执行（已拦截，未产生提交，误设的本地 user.name 已清除）；HTTPS 无凭据，最终经 SSH（id_ed25519_github → Hi winb0408）推送。后续更新流程存入长期记忆。

### 2026-09-15 #25：GitHub 快照补漏——README 实际未落盘 + runs/ 证据被 .gitignore 挡掉
- **发现**：用户找不到 README 后核查,两个问题:①#24 所报"仓库根 README.md"当时因工具故障**从未写入暂存区**,GitHub 上一直缺失(误报,纠正);②暂存区 `ACES/.gitignore` 的 `runs/` 规则把全部 run 证据挡在版本库外,GitHub 上只有代码/配置/文档(128 文件)——"精选证据入库"同样未达成。
- **修复**：README.md 先落 `/data2/wjb/Denoising_PM/README.md`(项目根,后续 rsync 流程自然带走),再入暂存区;runs 证据 419 个 JSON/CSV(全部 <3M)以 `git add -f ACES/runs/` 强制入库(不动 ACES/.gitignore,避免污染工作目录原文件)。commit d4c6b7e 已推送。
- **教训**：报告"已推送/已写入"前应以 `git ls-files`/文件系统实查为准,不以工具调用成功为准。
